[package]
name = "pitch"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "Convert between musical notes, frequencies, and MIDI note numbers"
license = "MIT"

[profile.release]
lto = true
codegen-units = 1
strip = true
